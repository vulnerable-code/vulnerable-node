<h1>Contact</h1>
<% if (typeof sent !== 'undefined' && sent) { %>
  <div class="ok-box">Message received — ticket #<%= ticket %>. We answer within 2 business decades.</div>
<% } %>
<div class="two-col">
  <div>
    <form method="post" action="/contact" class="form">
      <label>Your email</label>
      <input name="email" type="text" placeholder="you@example.com" required>
      <label>Message</label>
      <textarea name="message" rows="6" placeholder="Tell us everything. Newlines welcome." required></textarea>
      <button type="submit">Send message</button>
    </form>
    <p class="muted">VULN hint: this endpoint logs your message verbatim. See lab 10 (log injection) — the username payload works here too.</p>
  </div>
  <div>
    <h2>Other ways to reach us</h2>
    <dl class="dl">
      <dt>Support</dt><dd>support@nodebazaar.local (read quarterly)</dd>
      <dt>Security</dt><dd>Please do not email. Open a PR with the fix — see any <code>SAFE:</code> comment.</dd>
      <dt>Office</dt><dd>The standing desk, second floor</dd>
      <dt>Hours</dt><dd>Whenever the containers are up</dd>
    </dl>
    <h2>Before you write</h2>
    <p class="muted">Many answers live in the <a href="/faq">FAQ</a>. Fewer live in the logs, but those are more entertaining.</p>
  </div>
</div>